import logo from '../logo.svg';

const Logo = () => {
    return (
        <div className="logo">
            <img src={logo} className="App-logo" alt="logo" />
        
        </div>
    );
};

export default Logo;